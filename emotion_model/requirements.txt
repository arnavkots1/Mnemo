# Audio Emotion Detection Model Training
# Requirements for training a lightweight emotion classifier for iOS Core ML

# Core ML and model conversion
coremltools>=7.0
onnx>=1.14.0
onnxruntime>=1.15.0

# PyTorch
torch>=2.0.0
torchaudio>=2.0.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# Data handling
numpy>=1.24.0
pandas>=2.0.0

# Training utilities
scikit-learn>=1.3.0
tqdm>=4.65.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Optional: for better audio augmentation (commented out due to Python version compatibility)
# audiomentations>=0.42.0

